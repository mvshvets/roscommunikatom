import React from 'react'
import { PageContent } from '../../core/components'

export const Profile = React.memo(() => {
    return (
        <PageContent>
            Profile
        </PageContent>
    )
})